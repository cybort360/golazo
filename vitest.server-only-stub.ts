// Test stub for the "server-only" package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so server-only modules
// (e.g. lib/predict/settle.ts) can be unit-tested with a mocked DB client.
export {};
