# Public RPC (no API key needed for balance reads)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Canonical site URL (used for share/OG image links). e.g. https://golazo.fun
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Token addresses (fill after pump.fun launch)
NEXT_PUBLIC_GOLAZO_TOKEN_ADDRESS=

# Public wallets (displayed on site, anyone can verify on Solscan)
NEXT_PUBLIC_PRIZE_POOL_WALLET=
NEXT_PUBLIC_BUYBACK_WALLET=
NEXT_PUBLIC_FUTURE_FUND_WALLET=

# Admin only
ADMIN_SECRET=
HELIUS_API_KEY=

# Live scores: free football-data.org token (register at football-data.org).
# Drives auto-populated live scores and results. If unset, the site falls back
# to manual result entry only — nothing breaks.
WORLD_CUP_API_TOKEN=

# Vercel KV
KV_REST_API_URL=
KV_REST_API_TOKEN=
